import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'isj.confidenzmoblie.app',
  appName: 'Confidenz_Mobile',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
